spring:
  datasource:
    url: jdbc:h2:mem:vegetable_forum_test;MODE=MySQL;NON_KEYWORDS=DAY;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  data:
    redis:
      host: localhost
      port: 6379

security:
  jwt:
    issuer: vegetable-forum-test
    secret: test-secret
    expire-seconds: 3600
