package indi.midreamsheep.vegetable.backend;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * 应用启动入口。
 */
@SpringBootApplication
public class VegetableForumBackendApplication {

    /**
     * 程序主入口。
     *
     * @param args 启动参数
     */
    public static void main(String[] args) {
        SpringApplication.run(VegetableForumBackendApplication.class, args);
    }

}
